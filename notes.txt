> Вероятно, для тестирования было отключено, что по достижении 3 ошибок игра заканчивается и я допустил три ошибки, но смог выиграть

В ТЗ написано, что можно допустить 3 ошибки, то есть игра заканчивается после 4-й допущеной ошибки.

> Можно же сделать изящней, например, поставить все правильные ответы первыми, а в множественных ответах придумать визуальную подсказку.

Я для отладки пометил правильные ответы зеленым.

> Непонятно почему проходит тест на генерацию случайного числа: 10 раз мы запускаем генерацию числа от 0 до i. Распределение в JS равномерное ...

Это простой тест, я 10 раз запускаю генерацию случайного числа и проверяю, что сгенерировано хотя бы два разных числа (то есть функция не выдает одно и то же число каждый раз). Да, если бы числа генерировались случайные, то с убывающей на каждой итерации вероятностью мы бы могли получать единственное число. Но в виду алгоритма генерации псевдослучайных чисел, на достаточно большой выборке (больше двух) мы гарантировано получим разные числа.

> level-genre-view.js управление плеером

С level-artist-view.js все просто, там один плеер. А тут как поступить? Сохранить элементы плееров в массив для каждой песни и обходить каждый раз этот массив вместо того, чтобы пользоваться селекторами?
